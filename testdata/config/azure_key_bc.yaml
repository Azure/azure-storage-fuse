logging:
  level: log_debug
  file-path: "blobfuse2-logs.txt"
  type: base

components:
  - libfuse
  - block_cache_new
  - attr_cache
  - azstorage

libfuse:
  attribute-expiration-sec: 300
  entry-expiration-sec: 300
  negative-entry-expiration-sec: 300
  ignore-open-flags: true

#block_cache:
#  block-size-mb: 8

attr_cache:
  timeout-sec: 3600
  
azstorage:
  type: { ACCOUNT_TYPE }
  endpoint: { ACCOUNT_ENDPOINT }
  use-http: { USE_HTTP }
  account-name: { TEMP_SYELETI_ACC_NAME }
  account-key: { TEMP_SYELETI_ACC_KEY }
  mode: key
  container: { 0 }
  tier: hot
