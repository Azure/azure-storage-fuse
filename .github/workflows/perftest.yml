name: Performance Testing
on:
  push:
    branches:
      - main
      - vibhansa/perftestrunner

permissions:
  issues: write
  pull-requests: write

jobs:
  PerfTesting:
    runs-on: [self-hosted, 1ES.Pool=blobfuse-github-pool]
    steps:
      - name: 'Host info'
        run: hostnamectl
        